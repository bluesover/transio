# Server Connection Visual Guide

## Step-by-Step Connection Process

### 1️⃣ Start the Server

```
┌─────────────────────────────────────┐
│   Terminal 1: Start Server         │
├─────────────────────────────────────┤
│ $ cd server                         │
│ $ npm start                         │
│                                     │
│ 🚀 Transio Saxon-HE API Server     │
│ 📍 http://localhost:3001            │
│ 🏥 Health: /api/health              │
│ ⚡ Transform: /api/transform        │
│ 📦 Saxon JAR: ✅ Found              │
│ ✅ Server ready!                    │
└─────────────────────────────────────┘
```

Keep this terminal open! The server must stay running.

---

### 2️⃣ Open the App

```
┌─────────────────────────────────────┐
│   Terminal 2: Start Client         │
├─────────────────────────────────────┤
│ $ npm run dev                       │
│                                     │
│ VITE ready in 450 ms                │
│ ➜ Local: http://localhost:5173     │
└─────────────────────────────────────┘
```

Open browser to http://localhost:5173

---

### 3️⃣ Configure Server in UI

```
┌───────────────────────────────────────────────┐
│  Transio App - http://localhost:5173         │
├───────────────────────────────────────────────┤
│                                               │
│  [Lightning] [Folder] [☁️] [?] [Settings]    │
│                          ^                    │
│                          |                    │
│                   Click This!                 │
│                                               │
└───────────────────────────────────────────────┘
```

---

### 4️⃣ Server Configuration Dialog

```
┌────────────────────────────────────────────────────────┐
│  ☁️  Server Configuration (Optional)                   │
├────────────────────────────────────────────────────────┤
│                                                        │
│  ┌──────────────────────────────────────────────┐    │
│  │ ℹ️ Why use a server?                          │    │
│  │ • Full XSLT 2.0/3.0 support                  │    │
│  │ • Better performance for large files         │    │
│  │ • Automatic fallback to client               │    │
│  └──────────────────────────────────────────────┘    │
│                                                        │
│  Enable Server-Side Processing    [○─────] OFF       │
│                                                        │
│  ┌─────────────────────────────────────────┐         │
│  │ Server API URL                          │         │
│  │ http://localhost:3001/api          │         │
│  └─────────────────────────────────────────┘         │
│                                                        │
│  [ Test Connection ]                                  │
│                                                        │
│  [Cancel]                [Save Configuration]         │
└────────────────────────────────────────────────────────┘
```

**Steps:**
1. ✅ Toggle ON "Enable Server-Side Processing"
2. ✅ Enter URL: `http://localhost:3001/api`
3. ✅ Click "Test Connection"
4. ✅ Wait for status...

---

### 5️⃣ Connection Test Flow

```
Browser                Server
   │                      │
   │  GET /api/health     │
   ├─────────────────────>│
   │                      │
   │                      ├─ Check Saxon JAR exists
   │                      ├─ Check Java available
   │                      │
   │  200 OK + JSON       │
   │<─────────────────────┤
   │                      │
   
Response:
{
  "status": "ok",
  "processor": "Saxon-HE",
  "version": "12.5"
}
```

---

### 6️⃣ Success State

```
┌────────────────────────────────────────────────────────┐
│  ☁️  Server Configuration (Optional)                   │
├────────────────────────────────────────────────────────┤
│                                                        │
│  Enable Server-Side Processing    [──────○] ON       │
│                                                        │
│  ┌─────────────────────────────────────────┐         │
│  │ Server API URL                          │         │
│  │ http://localhost:3001/api          │         │
│  └─────────────────────────────────────────┘         │
│                                                        │
│  [ Test Connection ]  ✅ Available                    │
│                                                        │
│  ┌──────────────────────────────────────────────┐    │
│  │ ✅ Server Connected                          │    │
│  │ Processor: Saxon-HE                          │    │
│  │ Version: 12.5                                │    │
│  └──────────────────────────────────────────────┘    │
│                                                        │
│  Prefer Server for XSLT 2.0/3.0  [──────○] ON       │
│                                                        │
│  [Cancel]                [Save Configuration]         │
└────────────────────────────────────────────────────────┘
```

---

### 7️⃣ Error States

#### Server Not Running

```
┌────────────────────────────────────────────────────────┐
│  [ Test Connection ]  ❌ Unavailable                   │
│                                                        │
│  ┌──────────────────────────────────────────────┐    │
│  │ ❌ Connection Failed                         │    │
│  │ Failed to fetch                              │    │
│  │                                              │    │
│  │ The app will continue using client-side      │    │
│  │ transformation.                              │    │
│  └──────────────────────────────────────────────┘    │
└────────────────────────────────────────────────────────┘

🔧 Fix: Start the server!
   $ cd server && npm start
```

#### Saxon Not Installed

```
┌────────────────────────────────────────────────────────┐
│  [ Test Connection ]  ❌ Unavailable                   │
│                                                        │
│  ┌──────────────────────────────────────────────┐    │
│  │ ❌ Connection Failed                         │    │
│  │ Saxon-HE not found. Run: npm run setup       │    │
│  │                                              │    │
│  │ The app will continue using client-side      │    │
│  │ transformation.                              │    │
│  └──────────────────────────────────────────────┘    │
└────────────────────────────────────────────────────────┘

🔧 Fix: Install Saxon!
   $ cd server && npm run setup
```

#### Wrong URL

```
┌────────────────────────────────────────────────────────┐
│  ┌─────────────────────────────────────────┐         │
│  │ Server API URL                          │         │
│  │ http://localhost:3000/api          │         │
│  └─────────────────────────────────────────┘         │
│                    ^^^^                                │
│                Wrong port!                             │
│                                                        │
│  [ Test Connection ]  ❌ Unavailable                   │
│                                                        │
│  ┌──────────────────────────────────────────────┐    │
│  │ ❌ Connection Failed                         │    │
│  │ Failed to fetch                              │    │
│  └──────────────────────────────────────────────┘    │
└────────────────────────────────────────────────────────┘

🔧 Fix: Use correct URL
   Default: http://localhost:3001/api
                            ^^^^ Server runs on 3001
```

---

## Complete Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                    Browser (localhost:5173)                  │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  Transio React App                                 │    │
│  │                                                    │    │
│  │  XSLT 1.0 → Browser XSLTProcessor (built-in)     │    │
│  │                                                    │    │
│  │  XSLT 2.0/3.0 →                                   │    │
│  │    ├─ Client: Saxon-JS (limited)                  │    │
│  │    └─ Server: HTTP request to API ────────┐      │    │
│  └────────────────────────────────────────────┼──────┘    │
└──────────────────────────────────────────────┼─────────────┘
                                               │
                                               │ HTTP
                                               │
┌──────────────────────────────────────────────┼─────────────┐
│                 Server (localhost:3001)      ▼             │
│                                                            │
│  ┌─────────────────────────────────────────────────┐     │
│  │  Express.js API                                 │     │
│  │                                                 │     │
│  │  POST /api/transform                            │     │
│  │    └─> Spawn Java Process                       │     │
│  │         └─> Saxon-HE 12.5 (saxon-he-12.5.jar)   │     │
│  │              └─> Full XSLT 2.0/3.0 Support      │     │
│  │                   └─> Return transformed XML    │     │
│  │                                                 │     │
│  │  GET /api/health                                │     │
│  │    └─> Check Saxon JAR exists                   │     │
│  │         └─> Return status                       │     │
│  └─────────────────────────────────────────────────┘     │
│                                                            │
│  Dependencies:                                             │
│  • Node.js 18+                                            │
│  • Java 11+                                               │
│  • Saxon-HE JAR file                                      │
└────────────────────────────────────────────────────────────┘
```

---

## Transformation Flow with Server

```
User clicks "Transform" button
        │
        ▼
   Check XSLT Version
        │
        ├─── XSLT 1.0 ──────────────────────┐
        │                                    ▼
        │                           Browser XSLTProcessor
        │                                    │
        │                                    ▼
        │                            ✅ Result (fast)
        │
        └─── XSLT 2.0/3.0 ───────────┐
                                      ▼
                          Server enabled & prefer server?
                                      │
                          ┌───────────┴───────────┐
                         Yes                     No
                          │                       │
                          ▼                       ▼
                 POST /api/transform      Saxon-JS (client)
                          │                       │
                   ┌──────┴──────┐               │
                Success      Failed               │
                   │             │                │
                   │             └────────────────┤
                   ▼                              ▼
            ✅ Server result              Saxon-JS fallback
                   │                              │
                   └──────────────┬───────────────┘
                                  ▼
                          Display in Output Panel
```

---

## Network Tab View (Browser DevTools)

When you click "Test Connection", you should see:

```
┌────────────────────────────────────────────────────────────┐
│ Name                 │ Status │ Type │ Size    │ Time       │
├──────────────────────┼────────┼──────┼─────────┼────────────┤
│ health               │ 200    │ xhr  │ 89 B    │ 12 ms      │
│                      │   ✅   │      │         │            │
└────────────────────────────────────────────────────────────┘

Headers:
  Request URL: http://localhost:3001/api/health
  Request Method: GET
  Status Code: 200 OK

Response:
{
  "status": "ok",
  "processor": "Saxon-HE",
  "version": "12.5"
}
```

If you see this ✅ everything is working!

---

## Common Mistakes

### ❌ Wrong: URL has trailing slash
```
http://localhost:3001/api/
                         ^
```

### ✅ Correct: No trailing slash
```
http://localhost:3001/api
```

---

### ❌ Wrong: Missing /api path
```
http://localhost:3001
```

### ✅ Correct: Include /api
```
http://localhost:3001/api
```

---

### ❌ Wrong: Server not started
```
Terminal is closed
Server process not running
```

### ✅ Correct: Server running in terminal
```
Terminal shows:
🚀 Transio Saxon-HE API Server
📍 Server running on http://localhost:3001
✅ Server ready for transformations!
```

---

## Checklist for Success

- [ ] Java installed (`java -version` works)
- [ ] Server dependencies installed (`cd server && npm install`)
- [ ] Saxon downloaded (`cd server && npm run setup`)
- [ ] Server running in terminal (`npm start`)
- [ ] Terminal shows "✅ Server ready"
- [ ] Browser app open (http://localhost:5173)
- [ ] Server config dialog open (click cloud icon)
- [ ] Server enabled (toggle ON)
- [ ] URL is `http://localhost:3001/api`
- [ ] Test connection shows ✅ Available
- [ ] Configuration saved

If all checked, you're good to go! 🎉

---

## Still Not Working?

See [SERVER_TROUBLESHOOTING.md](./SERVER_TROUBLESHOOTING.md) for detailed troubleshooting steps.
